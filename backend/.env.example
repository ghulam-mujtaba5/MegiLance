# ===================================
# MegiLance Backend - Environment Variables
# ===================================

# Application Settings
APP_NAME=MegiLance API
ENVIRONMENT=development  # development, staging, production

# CORS Origins (JSON array format for pydantic-settings)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","*"]

# Database Configuration
DATABASE_URL=postgresql+psycopg2://megilance:megilance_pw@db:5432/megilance_db

# For local JSON mock data (optional)
JSON_DATA_DIR=/data/db

# Security & JWT
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars-recommended
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=10080  # 7 days
JWT_ALGORITHM=HS256

# ===================================
# Oracle Cloud Infrastructure (OCI) Configuration (PRODUCTION - ALWAYS FREE)
# ===================================

# OCI Region (Always Free eligible regions: us-ashburn-1, us-phoenix-1, ap-seoul-1, etc.)
OCI_REGION=us-ashburn-1

# OCI CLI Profile (configured via `oci session authenticate`)
OCI_PROFILE=DEFAULT

# OCI Namespace (auto-detected if not provided)
# OCI_NAMESPACE=your-tenancy-namespace

# OCI Compartment ID (get from Oracle Cloud Console)
# OCI_COMPARTMENT_ID=ocid1.compartment.oc1..xxxxx

# OCI Object Storage Buckets (Always Free: 10GB storage)
OCI_BUCKET_ASSETS=megilance-assets
OCI_BUCKET_LOGS=megilance-logs
OCI_BUCKET_UPLOADS=megilance-uploads

# OCI Vault Secret ID (for storing sensitive data)
# OCI_VAULT_SECRET_ID=ocid1.vaultsecret.oc1..xxxxx

# Oracle Autonomous Database (Always Free: 20GB, 1 OCPU)
# Option 1: Oracle native connection (recommended for best performance)
# DATABASE_URL=oracle+cx_oracle://admin:YourPassword@(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.us-ashburn-1.oraclecloud.com))(connect_data=(service_name=megilancedb_high))(security=(ssl_server_dn_match=yes)))

# Option 2: PostgreSQL-compatible connection (if using ADB with PostgreSQL protocol)
# DATABASE_URL=postgresql+psycopg2://admin:YourPassword@adb.us-ashburn-1.oraclecloud.com:5432/megilancedb

# Oracle Instant Client (required for cx_Oracle)
# Download wallet from OCI Console and extract to ./oracle-wallet
# OCI_WALLET_LOCATION=/app/oracle-wallet
# TNS_ADMIN=/app/oracle-wallet

# ===================================
# Legacy AWS Configuration (DEPRECATED - Remove after full migration)
# ===================================

# AWS Credentials (Use IAM roles in production, not hardcoded keys)
# AWS_REGION=us-east-2
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS S3 Buckets (DEPRECATED - use OCI_BUCKET_* instead)
# S3_BUCKET_ASSETS=megilance-prod-assets
# S3_BUCKET_LOGS=megilance-prod-logs
# S3_BUCKET_UPLOADS=megilance-prod-uploads

# AWS RDS (DEPRECATED - use Oracle Autonomous Database instead)
# RDS_HOST=megilance-db.xxxx.us-east-2.rds.amazonaws.com
# RDS_PORT=5432
# RDS_DB_NAME=megilance_prod
# RDS_USERNAME=megilance_admin
# RDS_PASSWORD=stored-in-secrets-manager

# AWS Secrets Manager ARNs (DEPRECATED - use OCI Vault instead)
# DB_SECRET_ARN=arn:aws:secretsmanager:us-east-2:123456789012:secret:prod/megilance/db
# JWT_SECRET_ARN=arn:aws:secretsmanager:us-east-2:123456789012:secret:prod/megilance/jwt
# CIRCLE_API_SECRET_ARN=arn:aws:secretsmanager:us-east-2:123456789012:secret:prod/megilance/circle

# AWS SES (Email Service) (DEPRECATED)
# SES_REGION=us-east-2
# SES_FROM_EMAIL=noreply@megilance.com
# SES_CONFIGURATION_SET=megilance-emails

# AWS SNS (Notifications) (DEPRECATED)
# SNS_TOPIC_ARN=arn:aws:sns:us-east-2:123456789012:megilance-notifications

# ===================================
# External Services
# ===================================

# AI Service
# AI_SERVICE_URL=http://ai-backend:8001
# OPENAI_API_KEY=sk-stored-in-secrets-manager

# Blockchain & Crypto Payments
# BLOCKCHAIN_PROVIDER_URL=https://mainnet.infura.io/v3/YOUR-PROJECT-ID
# USDC_CONTRACT_ADDRESS=0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48
# CIRCLE_API_KEY=stored-in-secrets-manager

# Monitoring
# SENTRY_DSN=https://xxxx@sentry.io/xxxx
# LOG_LEVEL=INFO
