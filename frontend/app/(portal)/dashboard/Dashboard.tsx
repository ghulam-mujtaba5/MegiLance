// @AI-HINT: Generic portal dashboard — unused legacy component (page.tsx handles redirect directly)
// Kept as placeholder. The actual redirect logic lives in page.tsx.
'use client';

import React from 'react';

const Dashboard: React.FC = () => {
  return null;
};

export default Dashboard;
