// @AI-HINT: This is the Next.js home route. It delegates to the Home component, which uses per-component theming and styles only.
import Home from "./Home/Home";

export default function Page() {
  return <Home theme="light" />;
}
