services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - ./oracle-wallet-23ai:/app/oracle-wallet:ro
      - ./backend:/app
      - /app/__pycache__
    environment:
      - DATABASE_URL=oracle+oracledb://ADMIN:Bfw5ZvHQXjkDb!3lAa1!@megilanceai_high?wallet_location=/app/oracle-wallet&wallet_password=MegiLance2025!Wallet
      - OCI_REGION=eu-frankfurt-1
      - OCI_NAMESPACE=frj6px39shbv
      - OCI_BUCKET_NAME=megilance-storage
      - OCI_COMPARTMENT_ID=ocid1.tenancy.oc1..aaaaaaaa4qmr2dmzh46gxp2urvb5rdhbg2zjljzlhsqbamujhcjc234iilq
      - ORACLE_WALLET_LOCATION=/app/oracle-wallet
      - ORACLE_WALLET_PASSWORD=MegiLance2025!Wallet
      - SECRET_KEY=your-secret-key-change-in-production-min-32-chars-megilance-oracle-2025
      - FRONTEND_URL=http://localhost:3000
      - ENVIRONMENT=production
      - DEBUG=False
    networks:
      - megilance

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_BACKEND_URL=http://backend:8000
      - NEXT_TELEMETRY_DISABLED=1
    depends_on:
      - backend
    networks:
      - megilance

networks:
  megilance:
    driver: bridge
